spec:
  template:
    spec:
      containers:
        - name: {{.procName}}
          env:
            {{- range .env }}
            - name: {{.name}}
              value: {{.value}}
            {{- end }}